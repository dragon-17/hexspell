

let bla = 56;   // a comment
let text = "hello /* cmt in str*/ world". repeat(  45 + bla );

function test /* a bi comment with "string" */ ( a=45, b="AAA" ){
     let obj = {  name: "Max", age:  (42+5) * 2, addr:{ bbb:45 }  };
     const name =  obj["name"] + (1+(2+3));
     return a + b;
}

